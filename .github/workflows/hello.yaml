name: Hello world workflow

on: 
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: hello world
        uses: actions/checkout@v2
        
        

